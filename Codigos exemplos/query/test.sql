-- Add in table

INSERT INTO users (name, email, age) VALUE (
    "APAGA",
    "APAGA.medeiros@gmail.com",
    100
);

-- Delete in table

DELETE FROM users WHERE name = "APAGA";

-- Update in table

UPDATE users SET name = "Ana Liz" WHERE name = "Liz";

